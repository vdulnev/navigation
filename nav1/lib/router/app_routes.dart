/// All named route constants used by the app.
///
/// Using a central file makes it easy to see every navigation destination
/// at a glance and avoids hard-coded strings scattered across files.
abstract class AppRoutes {
  static const splash = '/';
  static const login = '/login';
  static const home = '/home';
  static const profile = '/profile';
  static const editProfile = '/profile/edit';
  static const settings = '/settings';
}
